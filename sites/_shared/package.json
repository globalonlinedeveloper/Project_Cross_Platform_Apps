{
  "name": "@nikatru/site-shared",
  "version": "0.1.0",
  "private": true,
  "description": "Shared Eleventy layer for NIKATRU sites: base layout, partials, design tokens, and the single-source app registry (_data/apps.json).",
  "type": "module",
  "scripts": {
    "build": "eleventy",
    "dev": "eleventy --serve"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.1.6"
  },
  "engines": {
    "node": ">=18"
  }
}
