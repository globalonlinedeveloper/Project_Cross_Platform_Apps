// Re-export shim: AppColors now lives in packages/design_system.
// Kept so the ~17 existing `core/theme/app_colors.dart` imports compile unchanged.
export 'package:nikatru_design_system/nikatru_design_system.dart' show AppColors;
